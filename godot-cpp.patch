diff --git a/SConstruct b/SConstruct
index 3ebdff9..72d9900 100644
--- a/SConstruct
+++ b/SConstruct
@@ -19,6 +19,13 @@ except Exception:
 
 env.PrependENVPath("PATH", os.getenv("PATH"))
 
+# For windows compilation with nix
+platform_arg = ARGUMENTS.get("platform", None)
+if platform_arg == "windows":
+    env.Append(CPPPATH=[os.getenv("MCFGTHREADS_INCLUDE_DIR")])
+    env.Append(LIBPATH=[os.getenv("MCFGTHREADS_LIB_DIR"), os.getenv("MCFGTHREADS_DLL_DIR")])
+    env.Append(LIBS=['mcfgthread'])
+
 # Custom options and profile flags.
 customs = ["custom.py"]
 try:
diff --git a/tools/godotcpp.py b/tools/godotcpp.py
index 7be9624..d9ca204 100644
--- a/tools/godotcpp.py
+++ b/tools/godotcpp.py
@@ -541,7 +541,7 @@ def _godot_cpp(env):
     )
 
     bindings = env.GodotCPPBindings(
-        env.Dir("."),
+        env.Dir("#"),
         [
             api_file,
             os.path.join(extension_dir, "gdextension_interface.h"),
@@ -553,12 +553,22 @@ def _godot_cpp(env):
         env.AlwaysBuild(bindings)
         env.NoCache(bindings)
 
+    # When godot-cpp lives outside the project tree (e.g. a read-only Nix store
+    # path), compile its sources into the project via a variant dir so object
+    # files are never written next to the sources.
+    src_root = env.Dir(".").srcnode().abspath
+    if src_root != env.Dir("#").abspath:
+        src_prefix = "#godot-cpp/src"
+        env.VariantDir(src_prefix, os.path.join(src_root, "src"), duplicate=0)
+    else:
+        src_prefix = "src"
+
     # Sources to compile
     sources = [
-        *env.Glob("src/*.cpp"),
-        *env.Glob("src/classes/*.cpp"),
-        *env.Glob("src/core/*.cpp"),
-        *env.Glob("src/variant/*.cpp"),
+        *env.Glob(src_prefix + "/*.cpp"),
+        *env.Glob(src_prefix + "/classes/*.cpp"),
+        *env.Glob(src_prefix + "/core/*.cpp"),
+        *env.Glob(src_prefix + "/variant/*.cpp"),
         *tuple(f for f in bindings if str(f).endswith(".cpp")),
     ]
 
@@ -567,7 +577,7 @@ def _godot_cpp(env):
         CPPPATH=[
             env.Dir(extension_dir),
             env.Dir("include").srcnode(),
-            env.Dir("gen/include"),
+            env.Dir("#gen/include"),
         ]
     )
 
@@ -575,7 +585,7 @@ def _godot_cpp(env):
     library_name = "libgodot-cpp" + env["suffix"] + env["LIBSUFFIX"]
 
     if env["build_library"]:
-        library = env.StaticLibrary(target=env.File("bin/%s" % library_name), source=sources)
+        library = env.StaticLibrary(target=env.File("#bin/%s" % library_name), source=sources)
         env.NoCache(library)
         default_args = [library]
 
@@ -585,5 +595,5 @@ def _godot_cpp(env):
 
         env.Default(*default_args)
 
-    env.AppendUnique(LIBS=[env.File("bin/%s" % library_name)])
+    env.AppendUnique(LIBS=[env.File("#bin/%s" % library_name)])
     return library
